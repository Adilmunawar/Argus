# Control plane — console, CLI, GitOps

Three surfaces, one truth (ADR-0021). Everything below is `platform/`.

## 1. The truth: `argus-gitops`

A private repository (`ZaraatDost/argus-gitops`, mirrored on Forgejo). Its `main` branch **is** production. Its `staging` branch is the staging tenant. The only process with write access to production systems is the reconciler (ADR-0023), and it only applies what a signed, reviewed commit says.

```
argus-gitops/
  clusters/
    site-a/
      hosts/hv-01.yaml … hv-05.yaml        # DSC v3 + OpenTofu inputs: roles, WDAC policy id, VLANs
      vms/sql-01.yaml, pg-01.yaml, …        # Hyper-V VM spec, Shielded, replica target
      servicefabric/cluster.yaml            # node types, seed nodes, upgrade policy
    site-b/ …
  apps/
    mills/
      app.yaml                              # SF application: version, instances, ports, gMSA, health policy
      config/production.env.tmpl            # env vars with {{ openbao:kv/mills/... }} references
      alerts.yaml                           # Prometheus rules scoped to the app
    loan/ … agis/ … console/ … caddy/ … nats/ … openbao/ … observability/ …
  storage/
    buckets.yaml                            # every SeaweedFS bucket: lock policy, lifecycle, replication, owners
  identity/
    gmsa.yaml                               # every gMSA and which hosts may retrieve it
    groups.yaml                             # console roles → AD groups; Tier membership
    openbao-policies/                       # HCL policies; auth roles per gMSA
  network/
    opnsense-rules.yaml · ipsec-rules.yaml · vlans.yaml · egress-allowlist.yaml
  policies/
    wdac/ · gpo/ · wazuh-rules/ · sysmon.xml
  ml/
    dagster-schedules.yaml · ray-serve.yaml
  environments/
    production.yaml · staging.yaml          # which app versions are live where
```

Every file is validated in CI by a schema (`platform/gitops/schemas/`). A PR that changes `identity/` or `policies/` requires two Tier 1 approvals; anything else, one.

## 2. The reconciler (`platform/console/src/Argus.Reconciler`)

A Service Fabric **stateful reliable service** (3 replicas, one primary) written in C#.

Loop, every 60 s and on webhook:
1. Fetch `main`; verify the HEAD commit signature against `identity/signing-keys.yaml`.
2. Build the **desired model** from YAML.
3. Read the **actual model**: Service Fabric client API (apps, versions, health), Hyper-V via WMI/PowerShell remoting over JEA (VMs, replicas), SeaweedFS admin API (buckets, policies), OpenBao API (policies, roles), AD via LDAP (gMSA hosts, group membership with TTL), DSC v3 `Get` on hosts (roles, features, WDAC id), OPNsense API (rules).
4. Diff. Emit the plan to `argus_console_events` and the console UI.
5. Apply in dependency order: identity → secrets refs → storage → network → hosts → VMs → SF apps → alerts. Each step idempotent; each failure stops the plan, marks the commit `failed`, alerts.
6. For SF applications: download the `.sfpkg` from `argus-artifacts` by SHA, verify its Authenticode signature, `Register-ServiceFabricApplicationType`, `Start-ServiceFabricApplicationUpgrade` with the app's health policy (`FailureAction=Rollback`, `HealthCheckStableDuration=2m`). This is `update.ps1`'s stop-rename-swap-healthcheck-rollback made native.
7. Report: `desired == actual` per resource; drift (something changed outside Git) is an alert and, for security-relevant resources, an automatic revert.

Break-glass: a Tier 0 human can pause the reconciler from the console (logged, paged) for manual repair; resuming reconciles Git over whatever was done by hand.

## 3. The console (`platform/console`)

### 3.1 Stack

`Argus.Console.Api` — .NET 10 minimal API, same conventions as the Mills API (bare camelCase records, RFC 9457 problems, `/api/v1/*`, rate limiting, OTel). `Argus.Console.Web` — Next.js 16 standalone, the Mills design system (Geist + display serif, cream/leaf palette, colour-blind-safe status colours). Deployed as one SF application `ArgusConsole`, behind Caddy at `console.zaraatdost.pk`, reachable only over the admin VPN. Login: AD FS OIDC (PKCE). Authorisation: AD groups → roles.

### 3.2 Roles

| Role | AD group | Can |
|---|---|---|
| Viewer | `Argus-Console-Viewers` | see everything; no writes |
| Operator | `Argus-Console-Operators` (Tier 1) | deploy approved versions, scale, restart, run runbooks, open PRs from forms |
| Approver | `Argus-Console-Approvers` (Tier 1) | approve production PRs generated by the console |
| Security | `Argus-Console-Security` | see audit, Wazuh, WDAC events; quarantine a host; rotate leases |
| Admin | `Argus-Console-Admins` (Tier 0, time-boxed) | pause reconciler, edit identity/policies PRs, break-glass |

### 3.3 Screens (v1 — parity with what WAC + Grafana cannot do)

| Screen | What it shows | Writes (always via a PR) |
|---|---|---|
| **Overview** | site health, SLOs, reconciler state, last deploy, open alerts, backup age, cert expiry, AWS-exit progress | — |
| **Applications** | per app: version live/staging, instances, health, p95, error rate, logs tail, dependencies graph from the map | deploy version, scale, restart, rollback |
| **Deployments** | pending PRs with diff, approvals, reconciler plan/apply log | approve, reject |
| **Storage** | buckets, size, lock status, replication lag, top prefixes | create bucket (form → `storage/buckets.yaml` PR), lifecycle rule |
| **Databases** | AG state, backup timeline, last verified restore, replica lag, top queries (from `sql_exporter`) | request dynamic credential (OpenBao lease, shown once), trigger ad-hoc backup |
| **Secrets** | OpenBao paths (names only), lease counts, rotation age, who read what (audit) | rotate, revoke leases, request time-boxed access |
| **Identity** | gMSAs, groups, tier membership with expiry, MFA coverage | grant time-boxed Tier membership (approval required) |
| **Hosts & VMs** | cluster nodes, S2D health, VM placement, replica health, patch age, WDAC mode | live-migrate, drain node, quarantine host, request reboot window |
| **Security** | Wazuh SCA score per host, open alerts, WDAC block events, CrowdSec bans, Suricata top signatures | acknowledge, quarantine, open incident runbook |
| **ML** | Dagster asset freshness, Ray cluster utilisation, MLflow latest runs, GPU memory | trigger a Dagster job, promote a model |
| **Runbooks** | every `docs/runbooks/*.md` rendered with parameter forms; executes JEA-scoped PowerShell on the target with a transcript | run |
| **Audit** | every action by anyone, forever, exportable | — |
| **Evidence** | monthly evidence pack generator | generate, sign, download |

Day one (Phase 0–2), before these screens exist: Windows Admin Center for hosts/VMs, Grafana for everything observable, GitHub for PRs. The console's first shipped screens are **Deployments** and **Applications**, because they are what `update.ps1` does today.

### 3.4 API surface (excerpt)

```
GET  /api/v1/health
GET  /api/v1/overview
GET  /api/v1/apps                 GET /api/v1/apps/{name}
POST /api/v1/apps/{name}/deploy   { version, environment }  → creates PR, returns { prUrl }
POST /api/v1/apps/{name}/scale    { instances }
POST /api/v1/apps/{name}/rollback
GET  /api/v1/deployments          POST /api/v1/deployments/{id}/approve
GET  /api/v1/storage/buckets      POST /api/v1/storage/buckets
GET  /api/v1/databases            POST /api/v1/databases/{name}/credentials  → OpenBao lease
GET  /api/v1/secrets/paths        POST /api/v1/secrets/{path}/rotate
GET  /api/v1/identity/grants      POST /api/v1/identity/grants  { group, principal, hours }
GET  /api/v1/hosts                POST /api/v1/hosts/{name}/quarantine
GET  /api/v1/security/alerts      POST /api/v1/security/alerts/{id}/ack
GET  /api/v1/runbooks             POST /api/v1/runbooks/{id}/run  { params }
GET  /api/v1/audit?since=
POST /api/v1/reconciler/pause     POST /api/v1/reconciler/resume
```

Every `POST` records `{actor, role, action, target, before, after, prUrl?}` in `argus_console_events` and Loki.

## 4. The CLI (`platform/cli`)

`Argus` PowerShell module (signed, in the WDAC policy) and a thin `argus` .NET tool — both are clients of the console API; neither has privileges of its own.

```powershell
Connect-Argus                                   # AD FS device-code login; token cached in DPAPI
Get-ArgusApp mills                                   # status
Publish-ArgusApp mills -Version 2026.09.08.1 -Environment production   # opens the PR, prints the URL
Approve-ArgusDeployment 1842
Get-ArgusBackup umairv3_db -Last 5
New-ArgusDatabaseCredential umairv3_db -Ttl 1h       # OpenBao lease, printed once
Grant-ArgusTierAccess -Group Argus-Tier0-Admins -Hours 2 -Reason "CA CRL renewal"
Invoke-ArgusRunbook sql-01-restore-drill -Param @{ Target = 'sql-drill-01' }
Get-ArgusAudit -Since (Get-Date).AddDays(-1)
```

`argus` mirrors these for the Linux hosts and CI (`argus app deploy mills --version …`). Claude Code's `zd-deploy` and `zd-ops` plugins call `argus`.

## 5. How a change flows (the whole loop)

```
1. Zayan merges a Mills PR on GitHub
2. Self-hosted Windows runner: build · test · scan · sign · package → argus-artifacts/mills/2026.09.08.1.sfpkg (WORM)
3. Runner opens PR #1842 on argus-gitops: environments/staging.yaml  mills: 2026.09.08.1
4. Reconciler applies staging within 60 s; SF rolling upgrade; health OK
5. Adil, from the console Deployments screen (or `Publish-ArgusApp … -Environment production`), promotes: PR #1843 on production.yaml
6. Approver (a different Tier 1 person) approves in the console
7. Reconciler: verify signatures → SF upgrade on production with rollback policy → status green → WhatsApp "mills 2026.09.08.1 live, p95 212 ms"
```

Wall clock from step 1 to step 7 with prompt approval: under 10 minutes. Rollback is `Publish-ArgusApp mills -Version <previous>` — a PR like any other.
