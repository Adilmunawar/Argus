{
  "name": "argus",
  "version": "0.7.0",
  "private": true,
  "description": "Argus: a sovereign, Windows-first private cloud. This manifest exists only to pin the console prototype's test dependencies.",
  "license": "MIT",
  "scripts": {
    "test": "node platform/console/prototype/tests/run-tests.js",
    "test:sandbox": "node platform/console/prototype/tests/sandbox.js",
    "test:all": "npm test && npm run test:sandbox && npm run test:stress",
    "test:stress": "node platform/console/prototype/tests/stress.js"
  },
  "devDependencies": {
    "acorn": "^8.18.0",
    "axe-core": "^4.10.2",
    "playwright": "^1.49.1"
  }
}
